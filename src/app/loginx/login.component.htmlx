  <h1>login</h1>
    <div>
      <label id='labelLoginId' for="loginId">Login Id</label>
      <input type="text" id="loginId" required [(ngModel)]="localAuthState!.loginId" name="loginId"
        aria-labelledby="labelLoginId"
      >
    </div>
    <div>
      <label id="labelPassword" for="password">Password</label>
      <input type="text" id="password" required [(ngModel)]="localAuthState!.password" name="password"
        aria-labelledby="labelPassword"
      >
    </div>
    <div></div>
    <button class='login' (click)="onLogin()">Login</button>