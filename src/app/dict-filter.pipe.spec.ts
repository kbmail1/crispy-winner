import { DictFilterPipe } from './dict-filter.pipe';

describe('DictFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DictFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
